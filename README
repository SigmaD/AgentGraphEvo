AgentGraphEvo

Evolutionary Model for tag Based Agents on a given Graph.

If no command line arguments are provided, the setup detailed by RCA will be used.
Otherwise, 3 or 4 command line arguments can be provided:
1.number of agents. Default: 100
2.total number of generations. Default: 30000
3.Number of potential donors in each evolutionary step. Default: 3
(4).filepath of network. If this is not provided, every agent will be connected to every 
other agent.

